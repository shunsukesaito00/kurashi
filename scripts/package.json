{
  "private": true,
  "type": "module",
  "scripts": {
    "check:demo-sync": "node check-demo-sync.mjs",
    "test:share-urls": "node verify-share-urls.mjs",
    "test": "npm run check:demo-sync && npm run test:share-urls"
  },
  "dependencies": {
    "playwright": "1.61.1"
  }
}
